// core.js

/*
Create a StorageManager class that selects the appropriate storage handler based on availability and user preference.
 It should have methods to set, get, and remove items, which internally use the selected storage handler.
 */
